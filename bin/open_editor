#!/usr/bin/env zsh

file="$1"
line="$2"

case "$(basename "${EDITOR%% *}")" in
  vim|nvim)
    exec $EDITOR +"$line" "$file"
    ;;
  code|cursor)
    exec $EDITOR -g "$file:$line"
    ;;
  *)
    exec $EDITOR "$file"
    ;;
esac